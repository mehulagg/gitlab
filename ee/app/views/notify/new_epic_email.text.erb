<%= _('%{user} created an epic: %{epicLink}') % { user: sanitize_name(@epic.author_name),
                                                  epicLink: url_for(group_epic_url(@epic.group, @epic)) }
%>

<% if @epic.assignee %>
  <%= assignees_label(@issue) %>
<% end %>

<% if @epic.description -%>
 <%= @epic.description %>
<% end %>
