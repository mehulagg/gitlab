query project($fullPath: ID!, $urls: [String!]) {
  project(fullPath: $fullPath) {
    validations: dastSiteValidations(normalizedTargetUrls: $urls) @client {
      edges {
        node {
          normalizedTargetUrl @client
          status @client
        }
      }
    }
  }
}
