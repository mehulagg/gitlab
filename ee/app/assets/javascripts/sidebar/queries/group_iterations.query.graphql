#import "./iteration.fragment.graphql"

query issueIterations($fullPath: ID!, $title: String, $state: IterationState) {
  workspace: group(fullPath: $fullPath) {
    __typename
    props: iterations(title: $title, state: $state) {
      __typename
      nodes {
        ...IterationFragment
        state
      }
    }
  }
}
