#import "./iteration.fragment.graphql"

query issueSprint($fullPath: ID!, $iid: String!) {
  project(fullPath: $fullPath) {
    issue(iid: $iid) {
      id
      iteration {
        ...IterationFragment
      }
    }
  }
}
