mutation dastProfileUpdate($input: DastProfileUpdateInput!, $runAfterCreate: Boolean!) {
  dastProfileUpdate(input: $input) {
    dastProfile {
      editPath
    }
    pipelineUrl @include(if: $runAfterCreate)
    errors
  }
}
