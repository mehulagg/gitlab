mutation updateIterationCadence($input: IterationCadenceUpdateInput!) {
  iterationCadenceUpdate(input: $input) {
    iterationCadence {
      id
      title
      automatic
      startDate
      durationInWeeks
      rollOver
      iterationsInAdvance
      description
    }
    errors
  }
}
