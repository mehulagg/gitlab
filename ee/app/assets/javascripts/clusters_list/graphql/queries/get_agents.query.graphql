#import "~/graphql_shared/fragments/pageInfo.fragment.graphql"

query getAgents(
  $defaultBranchName: String!
  $projectPath: ID!
  $first: Int
  $last: Int
  $before: String
  $after: String
) {
  project(fullPath: $projectPath) {
    clusterAgents(first: $first, last: $last, before: $before, after: $after) {
      nodes {
        id
        name
      }

      pageInfo {
        ...PageInfo
      }
    }

    repository {
      tree(path: ".gitlab/agents", ref: $defaultBranchName) {
        trees(first: $first, last: $last, before: $before, after: $after) {
          nodes {
            name
            path
            webPath
          }

          pageInfo {
            ...PageInfo
          }
        }
      }
    }
  }
}
