#import "../fragments/integration_item.fragment.graphql"

mutation httpIntegrationDestroy($id: ID!) {
  httpIntegrationDestroy(input: { id: $id }) {
    errors
    integration {
      ...IntegrationItem
    }
  }
}
