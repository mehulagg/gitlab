#import "../fragments/integration_item.fragment.graphql"

mutation httpIntegrationResetToken($id: ID!) {
  httpIntegrationResetToken(input: { id: $id }) {
    errors
    integration {
      ...IntegrationItem
    }
  }
}
