#import "../fragments/integration_item.fragment.graphql"

mutation prometheusIntegrationUpdate($id: ID!, $apiUrl: String!, $active: Boolean!) {
  prometheusIntegrationUpdate(input: { id: $id, apiUrl: $apiUrl, active: $active }) {
    errors
    integration {
      ...IntegrationItem
    }
  }
}
