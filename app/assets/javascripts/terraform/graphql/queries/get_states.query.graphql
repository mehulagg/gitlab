#import "../fragments/state.fragment.graphql"

query getStates($projectPath: ID!, $before: String, $after: String) {
  project(fullPath: $projectPath) {
    terraformStates(first: 3, before: $before, after: $after) {
      count
      nodes {
        ...State
      }
    }
  }
}
