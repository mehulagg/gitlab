query getStates($projectPath: ID!) {
  project(fullPath: $projectPath) {
    terraformStates {
      nodes {
        id
        name
        lockedAt
        createdAt
        updatedAt

        lockedByUser {
          id
        }
      }
    }
  }
}
