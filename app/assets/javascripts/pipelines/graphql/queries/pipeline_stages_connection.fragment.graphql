fragment PipelineStagesConnection on CiConfigStageConnection {
  nodes {
    name
    groups {
      nodes {
        name
        jobs {
          nodes {
            name
            allowFailure
            environment
            script
            afterScript
            beforeScript
            when
            needs {
              nodes {
                name
              }
            }
          }
        }
      }
    }
  }
}
