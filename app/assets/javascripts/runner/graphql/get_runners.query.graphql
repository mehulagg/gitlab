query getRunners($status: CiRunnerStatus, $type: CiRunnerType) {
  runners(status: $status, type: $type) {
    nodes {
      id
      description
      runnerType
      shortSha
      version
      revision
      ipAddress
      active
      locked
      tagList
      contactedAt
    }
  }
}
