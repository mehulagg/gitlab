export default ({
  members,
  sourceId,
  currentUserId,
  canManageMembers,
  tableFields,
  tableAttrs,
  tableSortableFields,
  memberPath,
  requestFormatter,
  filteredSearchBarOptions,
}) => ({
  members,
  sourceId,
  currentUserId,
  canManageMembers,
  tableFields,
  tableAttrs,
  tableSortableFields,
  memberPath,
  requestFormatter,
  filteredSearchBarOptions,
  showError: false,
  errorMessage: '',
  removeGroupLinkModalVisible: false,
  groupLinkToRemove: null,
});
