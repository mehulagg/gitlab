#import "../fragments/detail_item.fragment.graphql"

query alertDetails($fullPath: ID!, $alertId: String, $fetchEnvironment: Boolean = false) {
  project(fullPath: $fullPath) {
    alertManagementAlerts(iid: $alertId) {
      nodes {
        ...AlertDetailItem
      }
    }
  }
}
