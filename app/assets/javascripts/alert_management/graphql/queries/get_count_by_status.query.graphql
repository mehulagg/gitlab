query getAlertsCount($searchTerm: String, $projectPath: ID!, $assigneeUsernames: String = "") {
  project(fullPath: $projectPath) {
    alertManagementAlertStatusCounts(search: $searchTerm, assigneeUsername: $assigneeUsernames) {
      all
      open
      acknowledged
      resolved
      triggered
    }
  }
}
