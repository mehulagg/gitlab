- anchors = local_assigns.fetch(:anchors, [])
- project_buttons = local_assigns.fetch(:project_buttons, false)

- return unless anchors.any?
%ul.nav
  - anchors.each do |anchor|
    - anchor_attrs = { class: anchor.is_link ? 'nav-link stat-link d-flex align-items-center' : "nav-link btn btn-#{anchor.class_modifier || 'missing'} d-flex align-items-center" }
    - anchor_attrs[:itemprop] = anchor.itemprop if anchor.itemprop
    %li.nav-item
      = link_to_if(anchor.link, anchor.label, anchor.link, anchor_attrs) do
        .stat-text.d-flex.align-items-center{ class: ('btn btn-default disabled' if project_buttons) }= anchor.label
