.context-header
  - if Feature.enabled?(:sidebar_refactor, current_user)
    = nav_link(**scope_menu.active_routes) do
      = render 'shared/nav/scope_menu_body', scope_menu: scope_menu
  - else
    = render 'shared/nav/scope_menu_body', scope_menu: scope_menu
