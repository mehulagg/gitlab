memory-gc-stats:
  extends:
    - .default-before_script
    - .use-pg11
  stage: prepare
  script:
    - ./collect_gc_stats.rb | tee stats.csv
  artifacts:
    paths:
      - stats.csv
    expire_in: 14d
