memory-gc-stats:
  extends:
    - .default-before_script
    - .use-pg11
  stage: prepare
  script:
    - scripts/perf/gc/collect_gc_stats.rb | tee tmp/stats.csv
  artifacts:
    paths:
      - tmp/stats.csv
    expire_in: 14d
