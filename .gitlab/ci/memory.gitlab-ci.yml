memory-gc-stats:
  extends:
    - .default-before_script
    - .use-pg11
  stage: prepare
  script:
    - mkdir -p tmp/gc_settings_exp
    - ./collect_gc_stats.rb
  artifacts:
    paths:
      - tmp/gc_settings_exp/*
    expire_in: 14d
