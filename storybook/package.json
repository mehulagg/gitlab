{
  "name": "gitlab-storybook",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile-gitlab-css": "cd .. && bin/rake assets:precompile",
    "start": "start-storybook -p 6006 -c config",
    "build": "build-storybook -c config",
    "postinstall": "yarn run compile-gitlab-css"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@storybook/addon-controls": "^6.1.20",
    "@storybook/addon-essentials": "^6.1.20",
    "@storybook/addon-links": "^6.1.20",
    "@storybook/vue": "^6.1.20"
  }
}
